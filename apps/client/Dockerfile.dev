# Development Dockerfile for Pokecat Hunt Frontend
FROM node:18

WORKDIR /app

# Copy dependency manifests
COPY package*.json ./

# Install dependencies (includes devDependencies)
RUN npm install

# Copy all source files
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Run the development server
CMD ["npm", "run", "dev"]
